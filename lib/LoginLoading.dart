import 'package:flutter/material.dart';

class LoadingLoading extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Center(
        child: CircularProgressIndicator(),
      );
}